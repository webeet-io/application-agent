# Direct Outreach Provider Notes

## Contact Discovery
Planned providers (choose one):
- Apollo
- Hunter.io
- Clearbit

Current status:
- Adapter is stubbed and always returns `provider_unavailable` until a provider is configured.

## Email Validation
Planned providers (choose one):
- ZeroBounce
- NeverBounce

Current status:
- Adapter is stubbed and always returns `provider_unavailable` until a provider is configured.

## Drafting
- OpenAI API key required for draft generation.
- Adapter is stubbed and returns `llm_failed` until a provider is configured.

## Current Status
No provider keys are configured yet. Implementation remains stubbed until keys are available.
